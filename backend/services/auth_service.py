"""
Auth service — password hashing, JWT creation/verification, role guards.
"""

# TODO: Implement:
# - hash_password(password: str) -> str
# - verify_password(plain: str, hashed: str) -> bool
# - create_access_token(user_id: int, role: str) -> str
# - decode_access_token(token: str) -> dict
# - get_current_user dependency (FastAPI Depends)
# - require_admin dependency (FastAPI Depends)
# - require_member dependency (FastAPI Depends)
