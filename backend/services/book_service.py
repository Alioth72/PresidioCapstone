"""
Book service — catalog CRUD and search logic.
"""

# TODO: Implement:
# - list_books(db, search, category, page, limit) -> list[Book]
# - get_book(db, book_id) -> Book
# - create_book(db, data: BookCreate) -> Book
# - update_book(db, book_id, data: BookUpdate) -> Book
# - delete_book(db, book_id) -> None
# - search_books(db, query: str) -> list[Book]  (fuzzy/semantic search for AI)
