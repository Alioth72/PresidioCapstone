"""
Loan service — borrowing, returning, and loan management logic.
"""

# TODO: Implement:
# - borrow_book(db, user_id, book_id) -> Loan
# - return_book(db, user_id, loan_id) -> Loan
# - get_user_loans(db, user_id, active_only) -> list[Loan]
# - get_all_loans(db, active_only) -> list[Loan]
# - get_overdue_loans(db, user_id=None) -> list[Loan]
# - close_loan(db, loan_id) -> Loan  (admin force-close)
